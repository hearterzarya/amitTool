// Simple Cookie Copier Extension - Background Service Worker

// Minimal background script - just logs that extension is loaded
console.log('Cookie Copier Extension loaded');

// No message handlers needed - popup.js handles everything directly
